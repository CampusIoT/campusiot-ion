//import { Pipe, PipeTransform } from '@angular/core';

//@Pipe({
//  name: 'hexstring',
//  pure: false
//})
//export class HexstringPipe implements PipeTransform {
//  constructor() { }

//  transform(base64: string): any {
//    return base64;
//    return Array.from(atob(base64), function(byte) {
//      return ('0' + (byte & 0xFF).toString(16)).slice(-2);
//    }).join('');
    //return this.atob(base64);
//  }
//}
